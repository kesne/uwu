# Migration `watch-20191210150724`

This migration has been generated by Jordan Gensler at 12/10/2019, 3:07:24 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_TokenGrant" (
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "id" TEXT NOT NULL   ,
  "redeemed" INTEGER NOT NULL DEFAULT 0  ,
  "tokens" INTEGER NOT NULL DEFAULT 0  ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "user" TEXT    REFERENCES "User"(id) ON DELETE SET NULL,
  PRIMARY KEY ("id")
);

INSERT INTO "new_TokenGrant" ("createdAt","id","redeemed","tokens","updatedAt","user") SELECT "createdAt","id","redeemed","tokens","updatedAt","user" from "TokenGrant"

DROP TABLE "quaint"."TokenGrant";

ALTER TABLE "quaint"."new_TokenGrant" RENAME TO "TokenGrant";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;

PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_User" (
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "email" TEXT    ,
  "id" TEXT NOT NULL   ,
  "name" TEXT    ,
  "twitchID" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_User" ("createdAt","id","name","twitchID") SELECT "createdAt","id","name","twitchID" from "User"

DROP TABLE "quaint"."User";

ALTER TABLE "quaint"."new_User" RENAME TO "User";

CREATE UNIQUE INDEX "quaint"."User.twitchID" ON "User"("twitchID")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration watch-20191210150538..watch-20191210150724
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:dev.db"
 }
 model TokenGrant {
   id        String   @default(uuid()) @id
@@ -19,6 +19,7 @@
   id          String       @default(uuid()) @id
   createdAt   DateTime     @default(now())
   twitchID    String       @unique
   name        String?
+  email       String?
   tokenGrants TokenGrant[]
 }
```


